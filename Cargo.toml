[package]
name = "lunarOS"
version = "0.1.0"
edition = "2024"
author  = ["Atul"]

[dependencies]


[profile.dev]
panic = "abort" # disable stack unwinding on panic

# the profile used for `cargo build --release`
[profile.release]
panic = "abort" # disable stack unwinding on panic